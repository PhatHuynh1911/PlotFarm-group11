import { useState } from 'react'
import { submitContact } from '../api.js'
function ContactSection() { const [submitted, setSubmitted] = useState(false); const [loading, setLoading] = useState(false); const [error, setError] = useState(''); const submit = async (event) => { event.preventDefault(); setLoading(true); setError(''); const form = new FormData(event.currentTarget); try { await submitContact({ ho_va_ten: form.get('name'), so_dien_thoai: form.get('phone') }); setSubmitted(true) } catch (requestError) { setError(requestError.message) } finally { setLoading(false) } }; return <section className="contact-section" id="contact"><div className="contact-inner"><div><p className="eyebrow light">Sẵn sàng bắt đầu?</p><h2>Để PlotFarm<br /><em>giữ chỗ cho bạn.</em></h2><p className="contact-subtitle">Để lại thông tin, đội ngũ của chúng mình sẽ gọi lại trong vòng 24 giờ.</p></div>{submitted ? <div className="success-message"><span>✓</span><h3>Cảm ơn bạn đã đăng ký!</h3><p>PlotFarm sẽ liên hệ với bạn sớm nhất.</p></div> : <form className="contact-form" onSubmit={submit}><label>Họ và tên<input name="name" required placeholder="Nguyễn Minh An" /></label><label>Số điện thoại<input name="phone" required type="tel" placeholder="09xx xxx xxx" /></label>{error && <p className="dashboard-error" role="alert">{error}</p>}<button className="primary-button light-button" disabled={loading} type="submit">{loading ? 'Đang gửi...' : 'Nhận tư vấn miễn phí'} <span>→</span></button></form>}</div></section> }
export default ContactSection
